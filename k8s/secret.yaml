# TEMPLATE - Replace placeholder values before applying.
# In production, use AWS Secrets Manager + External Secrets Operator
# or Sealed Secrets (https://github.com/bitnami-labs/sealed-secrets).
# NEVER commit real credentials to version control.
apiVersion: v1
kind: Secret
metadata:
  name: refacil-wallet-secrets
  namespace: refacil-wallet
  labels:
    app: refacil-wallet
type: Opaque
stringData:
  DATABASE_URL: "postgresql://USER:PASSWORD@RDS_ENDPOINT:5432/wallet"
